
export const environment = {
  production: false,
  apiUrl: '/api',                        // para HTTP (proxy de Angular)
  wsUrl: 'http://localhost:3000',        // para WebSocket (directo al backend)
};